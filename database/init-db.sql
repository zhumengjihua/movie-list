
CREATE TABLE `jav` (
  `id` integer PRIMARY KEY,
  `code` text,
  `j_title` text,
  `c_title` text,
  `fid` text,
  `cid` text,
  `pc` text,
  `play_long` integer,
  `te` bigint,
  `stars` text,
  `tags` text,
  `publish_date` text,
  `cover_url` text
);